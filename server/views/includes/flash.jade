if messages.success
	#flash-message
		.alert.alert-success.animated.fadeIn(role='alert')
			button.close(type='button', data-dismiss='alert') ×
			for success in messages.success
				div= success

if messages.info
	#flash-message
		.alert.alert-info.animated.fadeIn(role='alert')
			button.close(type='button', data-dismiss='alert') ×
			for info in messages.info
				div= info

if messages.error
	#flash-message
		.alert.alert-danger.animated.fadeIn(role='alert')
			button.close(type='button', data-dismiss='alert') ×
			for error in messages.error
				div= error
